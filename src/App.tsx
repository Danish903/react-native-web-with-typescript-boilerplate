import React from "react";
import { Text, View } from "react-native";

const App: React.FC = () => {
   return (
      <View>
         <Text>hello from web</Text>
      </View>
   );
};

export default App;
